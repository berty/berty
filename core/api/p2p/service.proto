syntax = "proto3";

package berty.p2p;

option go_package = "berty.tech/core/api/p2p";

import "api/p2p/envelope.proto";

service Service {
  // HandleEnvelope is the default p2p handler, it receives an envelope containing an encrypted event
  rpc HandleEnvelope(Envelope) returns (Void) {};

  rpc Ping(Void) returns (Void) {};
}

message Void {};
